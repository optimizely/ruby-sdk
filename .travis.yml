language: ruby
rvm:
  - 2.0.0
before_install: gem install bundler -v 1.10.6
branches:
  only:
  - master
install:
  - bundle install
  - bundle exec rake install
script: "rake spec"
after_success: "coveralls"
